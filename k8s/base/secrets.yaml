apiVersion: v1
kind: Secret
metadata:
  name: gnani-secrets
type: Opaque
stringData:
  # Placeholder values - these should be replaced in actual deployment or managing via SealedSecrets/Vault
  mongodb-uri: "mongodb://user:password@mongodb:27017/gnani"
  redis-url: "redis://:password@redis:6379"
  jwt-secret: "change-me-in-production"
  encryption-key: "change-me-in-production"
  mongodb-username: "user"
  mongodb-password: "password"
  redis-password: "password"
