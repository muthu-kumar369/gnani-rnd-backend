// index.js
// This file serves as the main entry point for the GNANI backend application.
// It is responsible for initiating the core application logic defined in src/app.js.

require('./src/app');

// The src/app.js file handles the initialization of the Express.js and gRPC servers,
// as well as the connection to the MongoDB database.
// All server startup logs and error handling are managed within src/app.js and its modules.