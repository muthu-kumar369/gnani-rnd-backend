// backend/src/proto/gnani.proto
syntax = "proto3";

package gnani;

service GnaniService {
  rpc StartSession (StartSessionRequest) returns (StartSessionResponse);
  rpc SendAudioStream (stream SendAudioStreamRequest) returns (stream SendAudioStreamResponse);
  rpc EndSession (EndSessionRequest) returns (EndSessionResponse);
}

message StartSessionRequest {
  string user_id = 1;
  string session_id = 2;
}

message StartSessionResponse {
  bool success = 1;
  string message = 2;
  string session_id = 3;
}

message SendAudioStreamRequest {
  string session_id = 1;
  bytes audio_chunk = 2;
  bool end_of_stream = 3;
}

message SendAudioStreamResponse {
  string session_id = 1;
  string transcription = 2; // Placeholder for ASR output
  string llm_response = 3; // Placeholder for LLM output
  string action_directive = 4; // Placeholder for action service
}

message EndSessionRequest {
  string session_id = 1;
}

message EndSessionResponse {
  bool success = 1;
  string message = 2;
  string session_summary = 3;
}
